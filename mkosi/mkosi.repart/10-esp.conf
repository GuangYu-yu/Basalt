# ESP — 存放 UKI（*.efi）。构建期自适应：repart 在 [Min,Max] 内按内容实际大小
# 产出（单份 UKI），build.sh 提取后 ×ESP_SLOTS（A/B + fallback 预留）二次
# repart 定稿。此处仅设上下限兜底。
[Partition]
Type=esp
Label=ESP
Format=vfat
SizeMinBytes=64M
SizeMaxBytes=1G
