# var — 用户数据分区（btrfs：快照/zstd 压缩/校验和）
# 构建期最小化；部署后首启由 initrd 内 repart 扩满分区，
# x-systemd.growfs 在真实根挂载点完成文件系统在线扩展。
# 压缩挂载选项由此处生成 fstab 条目 —— 唯一事实来源。
[Partition]
Type=var
Label=var
Format=btrfs
CopyFiles=/var
Minimize=guess
MountPoint=/var compress=zstd:1,noatime,x-systemd.growfs 0 0
