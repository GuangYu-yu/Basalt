# 官方 root-verity.md 同范式：initrd 内的 repart 在 sysroot 挂载后运行，
# 消费 /sysroot/usr/lib/repart.d 中的设备侧定义（90-root-a / 91-root-b /
# 92-var-grow；B 槽尺寸由 build.sh 自适应渲染）
# 完成 B 槽/var 分区扩容 —— 此刻两分区均未挂载，纯分区表操作，零在线风险。
[Unit]
After=sysroot.mount
ConditionDirectoryNotEmpty=|/sysroot/usr/lib/repart.d