# 挂钩 initrd-switch-root：overlay 组装完成后才允许换根。
# Wants（非 Requires）不阻塞 switch-root 时序；组装失败无回退——
# /sysroot 仍是空 @os（非可启动根），switch-root 失败落 initrd emergency
# （见 initrd-root-overlay.service 头注），只读根继续启动仅 rescue
# 形态（basalt.ro=1）成立。
[Unit]
Wants=initrd-root-overlay.service
After=initrd-root-overlay.service