# emergency 取证转储（见 emergency-dump 脚本头注）：sysroot 挂载失败进
# emergency 时自动把池子卷账目 + ESP UKI 清单 + journal 尾部打到 console。
# drop-in 于 initrd 子镜像内（emergency.service 属 initrd 树，真实根的
# emergency 由另一棵树承载——sysroot 失败的 emergency 只发生在 initrd）。
[Service]
ExecStartPre=-/usr/lib/basalt/emergency-dump
