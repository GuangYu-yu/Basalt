# B 槽定义：SizeMinBytes 由 build.sh 在自适应定稿阶段以实测 A 槽分区大小
# 渲染（B = A，两槽同角色，零魔数）。本仓库版本为未暂存兜底：
# 无尺寸指令 → 对 B 槽 no-op（等价于"不扩容"，仅在构建脚本未运行时出现）。
# 首次 sysupdate 后 B 槽标签变为版本标签；本定义仍按 Type 序配对 B 槽，
# 若后续版本 root 大于 B 槽则自动再扩 —— 自适应闭环。
[Partition]
Type=root
GrowFileSystem=no
