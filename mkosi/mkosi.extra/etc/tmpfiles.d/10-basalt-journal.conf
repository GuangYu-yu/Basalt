# journal 持久化（Storage=auto 需 /var/log/journal 在 journald 启动时已存在
# 才启用持久模式）。/var 运行时即 @data 挂载 → 目录落在 @data，跨部署持久。
# mkosi CopyFiles 的 .gitkeep 种子被排除（实测 @data 无此目录），tmpfiles
# 是确定性路径。
d /var/log/journal 0755 root root -
