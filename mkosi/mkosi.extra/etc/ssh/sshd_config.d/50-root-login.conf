# 就绪/数据平面测试与现场维护均用 root+密码登录（密码由构建期注入，
# 非烘焙固定值）。上游默认 prohibit-password 会拒绝密码认证导致 SSH
# 永远不可达；sshd_config.d 的 Include 在主文件前，先到先得
PermitRootLogin yes
PasswordAuthentication yes
# 局域网 appliance + 测试装置高频单命令会话，同源惩罚会误伤合法访问
# （表现为 banner exchange 超时）；暴力破解防护由局域网边界承担
PerSourcePenalties no