# Readiness/data-plane tests and on-site maintenance use root+password login
# (password injected at build time, not baked fixed). Upstream default
# prohibit-password rejects auth making SSH unreachable forever; sshd_config.d
# Include is before the main file, first-match-wins.
PermitRootLogin yes
PasswordAuthentication yes