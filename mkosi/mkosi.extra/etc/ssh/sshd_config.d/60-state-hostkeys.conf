# host key 持久层 = @data 的 state 目录（跨部署版本稳定，杜绝每次换部署
# 的 TOFU 密钥风暴）。HostKey 指令一经设置即完全取代编译默认路径（/etc/ssh），
# 密钥由 basalt-state-init.service 首启生成（先于本服务，排序见其单元）。
HostKey /var/lib/basalt/state/ssh/ssh_host_ed25519_key
HostKey /var/lib/basalt/state/ssh/ssh_host_rsa_key
