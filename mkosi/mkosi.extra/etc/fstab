# ESP：标签在 mkosi.repart/10-esp.conf 声明一次；固定挂载点是
# /usr/lib/sysupdate.d/80-uki.transfer 的 Path= 前提（单一事实派生）。
PARTLABEL=ESP /efi vfat umask=0077,noatime 0 0
# @os 运行期挂载点：overlay upper/work 层的宿主（initrd 期同卷挂 /sysroot 组装 overlay）。
# 显式 rw：与 build.sh rootflags 同源（systemd 缺 rw/ro 时挂载为 ro）。
PARTLABEL=var /var/lib/basalt btrfs subvol=@os,compress=zstd:1,noatime,rw 0 0
# @images：版本化 EROFS 镜像。只读保护为 btrfs 子卷属性（ro=true，persistent），
# 非挂载层 ro——同 superblock 下 @os/@data 仍 rw 时 mount remount,ro 必 EBUSY
# （实测），故 VFS 挂载保持 rw，只读由属性承载：每次 boot 由
# images-lock.service 强制收敛 ro=true；OTA 写入经 systemd-sysupdate.service
# 的 wrapper drop-in 临时 ro=false → trap 恢复 true
# （usr/lib/systemd/system/systemd-sysupdate.service.d/10-images-rw.conf）。
# 手动 sysupdate update/vacuum 须先 btrfs property set -ts ... ro false。
PARTLABEL=var /var/lib/basalt/images btrfs subvol=@images,compress=zstd:1,noatime 0 0
# /var（@data）：手写 PARTLABEL=var（与其余条目统一），刻意不用 repart
# MountPoint= 自动生成的 fs UUID 段——两阶段构建（pass1 EROFS 固化 fstab /
# pass2 最终盘 mkfs）产生两次独立 fs UUID，fstab 固化值与盘上实际失配导致
# fsck 等不到设备。growfs 由本条目 x-systemd.growfs
# 触发，分区级 grow 由 initrd 92-var-grow.conf 承担。
PARTLABEL=var /var btrfs subvol=@data,compress=zstd:1,noatime,x-systemd.growfs 0 0
# / 的 overlay 组装在 initrd 完成（见 mkosi.images/initrd/.../initrd-root-overlay.service）：
#   fstab-generator 对 / 只做 remount 语义，真实根阶段无法换 fstype ——
#   此处刻意无 / 条目（systemd-remount-fs 因此对 overlay 根零操作，干净）。
