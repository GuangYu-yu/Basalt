# ESP：标签在 mkosi.repart/10-esp.conf 声明一次；固定挂载点是
# /usr/lib/sysupdate.d/80-uki.transfer 的 Path= 前提（单一事实派生）。
PARTLABEL=ESP /efi vfat umask=0077,noatime 0 0
# /var（@data）：手写 PARTLABEL=var（与其余条目统一），刻意不用 repart
# MountPoint= 自动生成的 fs UUID 段——两阶段构建（pass1 tar 固化 / pass2
# 最终盘 mkfs）产生独立 fstab 状态，fstab 固化值与盘上实际失配导致 fsck
# 等不到设备。growfs 由本条目 x-systemd.growfs 触发，分区级 grow 由 initrd
# 92-var-grow.conf 承担。
PARTLABEL=var /var btrfs subvol=@data,compress=zstd:1,noatime,x-systemd.growfs 0 0
# @landscape：应用载荷子卷（landscape-webserver + static/ + 工厂默认 toml），
# 与 root OTA 解耦独立更新（basalt-landscape-update.service）。挂载点目录在
# @data 内，缺失时由 systemd mount 单元自动创建（mount.c mkdir_p_label）。
PARTLABEL=var /var/lib/basalt/landscape btrfs subvol=@landscape,compress=zstd:1,noatime 0 0
# 池顶层（subvolid=5，规范地址——比名字/路径写法稳）：sysupdate 建部署子卷的
# 落点。缺此挂载时 sysupdate 的 btrfs subvolume create 落在调用进程根
# （运行部署内部）→ 嵌套子卷事故（UKI 顶层寻址失败 / vacuum 连坐 / 嵌套
# 阻断删除，且 update 本身"成功"静默出错）。挂载必须 rw：btrfs subvolume
# create 在 ro 挂载上 EROFS 直接失败（elemental-operator #923 实证）。
# 该挂载是 @data 同级的普通数据卷暴露面（pool 视图可见全部子卷），无额外
# 保护需求；旧部署的 ro 保护在 btrfs 属性层（deployments-readonly.service）。
PARTLABEL=var /var/lib/basalt/pool btrfs subvolid=5,noatime 0 0
# / 无条目：sysroot.mount 由 cmdline root= + rootflags=subvol=root-basalt-<N>
# 在 initrd 内经 fstab-generator 生成（rw，全根可写）；systemd-remount-fs
# 对无 / 条目的根零操作。设备身份（machine-id/ssh key/网络片段）在
# /var/lib/basalt/state（@data 普通目录），无独立 fstab 条目。
