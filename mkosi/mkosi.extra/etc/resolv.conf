# Factory default DNS (appliance out-of-box; Landscape takes over after
# claiming interfaces). Image ships without systemd-resolved (see system-preset);
# base system needs a working resolver for apt/geoip/debugging.
nameserver 1.1.1.1
nameserver 8.8.8.8
options timeout:2 attempts:2