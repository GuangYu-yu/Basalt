# var(btrfs) 独占全部剩余空间（root 为 EROFS 不参与增长）。
# 真实根侧增长：initrd 早期 repart 对离线扩盘可见的 usable 仍是旧值
# （实测 PADDING=0B 两遍皆然），真实根则正确识别全部空闲并增长
# （实测 365M → 1.4G）。配合 fstab /var 的 x-systemd.growfs 完成文件
# 系统级扩展；设备与定义见 systemd-repart.service.d/10-var-grow.conf。
[Partition]
Type=var
Weight=100
