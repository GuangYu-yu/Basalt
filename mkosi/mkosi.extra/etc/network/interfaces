# Managed by Landscape Router - do not edit
# This file is versioned deployment content (/etc atomically updates with
# root-basalt-<v>); device-specific network state persists in @data state
# directory (stable across versions), pulled in via source.
# ifupdown source supports glob; silent-skips when directory missing/no matches
# (basalt-state-init creates skeleton). Runtime interfaces (eth*) are managed
# dynamically by landscape-router.
auto lo
iface lo inet loopback
source /var/lib/basalt/state/network/*.conf